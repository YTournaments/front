import { Container } from "@mui/material";
import React from "react";
import { appName } from "../constants";
const Landing = () => {
  return <Container>{appName}</Container>;
};

export default Landing;
